define([
  'core/chart',
  'core/component/component'
],
function (chart, component) {
  'use strict';

  var core = {
    version: '0.0.1',
    chart: chart,
    component: component
  };

  return core;
});
